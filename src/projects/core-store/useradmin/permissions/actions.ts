import { name } from './__init__';

/**
 * Set administrative permissions group list
 */
export const SET_ADMIN_PERMISSIONS = `${name}/SET_ADMIN_PERMISSIONS`;

/**
 * Set transactional permissions group list
 */
export const SET_TX_PERMISSIONS = `${name}/SET_TX_PERMISSIONS`;

/**
 * Set menu permissions group list
 */
export const SET_MENU_PERMISSIONS = `${name}/SET_MENU_PERMISSIONS`;
