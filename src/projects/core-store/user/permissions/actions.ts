/* Permissions Actions. */
import { name } from './__init__';

/* Set permissions list. */
export const SET_PERMISSIONS_LIST = `${name}/SET_PERMISSIONS_LIST`;

/* Requested flags. */
export const SET_PERMISSIONS_REQUESTED = `${name}/SET_PERMISSIONS_REQUESTED`;
export const RESET_PERMISSIONS_REQUESTED = `${name}/RESET_PERMISSIONS_REQUESTED`;
