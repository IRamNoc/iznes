/* Core imports. */
import {Injectable} from '@angular/core';

/* Decorator. */
@Injectable()

/* Class. */
export class PersistService {
    /* Constructor. */
    constructor () {
        /* Stub. */
    }
}
